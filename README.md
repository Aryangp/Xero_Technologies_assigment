## Note the frontend code is in different repository not in this one , a demo video in frontend readme

## Commands
```
create a virtual env
connect it with terminal
```
```
pip install -r requirements.txt
flask --app app --debug run 
```
